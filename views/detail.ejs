<!DOCTYPE HTML>

<html>
<head>
    <title>Project-任務列表</title>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no"/>
    <!--[if lte IE 8]>
    <script src="assets/js/ie/html5shiv.js"></script><![endif]-->
    <link rel="stylesheet" href="/assets/css/main.css"/>
    <!--[if lte IE 9]>
    <link rel="stylesheet" href="/assets/css/ie9.css"/><![endif]-->
    <!--[if lte IE 8]>
    <link rel="stylesheet" href="/assets/css/ie8.css"/><![endif]-->
</head>
<body>

<!-- Wrapper -->
<div id="wrapper">

    <!-- Header -->
    <% include ./header %>



	<table>
                  <tr>
                      <th>任務名稱</th>
                      <th>任務類別</th>
                      <th>佣金 NT$</th>


                  </tr>
                  <% if(task[0]){%>
                    
                      <tr>
                        <td><%=task[0].title%></td>
                        <td><%=task[0].category%></td>
                          <td><%=task[0].reward%></td>

                    <% }%>
                      </tr>
                    <tr>

                        <th>到期時間</th>
                        <th>等級限制</th>

                         <th>發布者</th>

                    </tr>
                        <% if(task[0]){%>
                        <tr>
                        <td><%=task[0].due_date%> <%=task.due_time%></td>
                        <td><%=task[0].limited_level%></td>

                        <td><%=task[0].pmID.nickname%></td>

                        <% }%>
                        </tr>
                     <tr>

                        <th>任務內容</th>

                        </tr>
                        <tr>

                            <% if(task[0]){%>

                            <tr>
                            <td><%- md.render(task[0].content) %></td>
                            <% }%>
                        </tr>
                       </tr>
                            
                    </table>


    <% if(task[0].pmID._id == current_member._id){%>
    <p>這是由您所發佈的任務</p>

    <%}else{ %>
        <form action="/request" id="requestForm" method="post">
            <input type="hidden"  id="tID"  name="tID" value="<%=task[0]._id%>">
            <input type="hidden"  id="rmID"  name="rmID" value="<%= current_member._id %>">
            <input type="hidden" id="status" name="status" value="request">
            <input type="submit" id="rtSubmit" name="rtSubmit" class="rtSubmit" value="接取任務">
        </form>
    <%}%>

</div>



</body>
</html>